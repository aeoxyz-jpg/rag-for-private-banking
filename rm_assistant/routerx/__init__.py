"""Pillar F routing-accuracy validation: classify the routing gold, build a confusion matrix and
per-route precision/recall. The thesis's load-bearing component, previously functional-only (§3.5)."""
